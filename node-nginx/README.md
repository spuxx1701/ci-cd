# Deployment via Docker, Node & NGINX
This setup builds and deploys an app via Docker, Node and NGINX. Remember to set the project name before using.

The following repository secrets need to be set:
* `DOCKERHUB_USER` The dockerhub username.
* `DOCKERHUB_TOKEN` The dockerhub access token.
